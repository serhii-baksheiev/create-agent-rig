{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:create-agent-rig:conformance:v1:doctor",
  "title": "Command contract v1.0 — `doctor --json`",
  "description": "A list of check records and their aggregate status. What this schema checks: every record carries all four fields, and the status words are the closed set; additive keys are tolerated. What it does not check: the contract's rule that no field but a record's human-facing fix names a file path — that stays a reviewed rule of docs/command-contract.md.",
  "type": "object",
  "required": ["schemaVersion", "status", "checks"],
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "status": { "type": "string", "enum": ["ok", "warn", "fail"] },
    "checks": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "status", "detail", "fix"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "status": { "type": "string", "enum": ["ok", "warn", "fail"] },
          "detail": { "type": "string" },
          "fix": { "type": "string" }
        }
      }
    }
  }
}
