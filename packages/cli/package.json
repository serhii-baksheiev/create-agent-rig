{
  "name": "@create-agent-rig/cli",
  "version": "0.6.2",
  "private": true,
  "type": "module",
  "description": "Generator sources. Built by the root `prepare`/`build` scripts into dist/, which the root package publishes as its bin.",
  "scripts": {
    "prepublishOnly": "node -e \"console.error('BLOCKED: this inner package is never published — publish the repo root (create-agent-rig).'); process.exit(1)\""
  }
}
