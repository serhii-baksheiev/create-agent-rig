# Journal — 2026-09

The human journal for September 2026. **Newest first** — a new entry goes
directly under this heading, so the file reads as a stack. Entries are date-free
by design; the month is the filename's job.

One file per month, `journal/YYYY-MM.md`. The convention and the field list live
in `templates/agent-os/universal/journal/README.md`, which is the copy a
generated project gets.

🔴 **This is not `.claude/runs/*/events.jsonl`.** That one is the machine trace —
append-only, oldest-first, written by `run-journal.mjs`. This file is the human
record, newest-on-top, written by a session at its checkpoints and stops.

### 0.7.0 went out while the repository still said it was waiting; the published tarball is provably the tree that built it; and the artifact-equivalence waiver was earned rather than assumed

- **done — 0.7.0 is on npm**, published by the owner 2026-09-01T19:02:58Z. This
  run did not publish it and could not: `npm publish` needs 2FA. What this run
  did was the post-publish half — step 9 of the `CHANGELOG.md` checklist — and
  then reconcile everything the publication made stale.

- **the published artifact is identified, not assumed.** `gitHead` is
  `6589db36`, exactly current `master`. The tarball was downloaded and re-hashed
  locally: sha1 `34c94881eb3d3aa7a2c985428e53670c3c7efe5d`, matching
  `dist.shasum`; 245 files, 1660553 bytes unpacked, matching `dist.fileCount`
  and `dist.unpackedSize`. Extracted and compared per-file against a local
  `npm pack` of `master`: **identical file set, identical per-file sha256**.

- 🔴 **tarball shasum is not a cross-host identity, and treating it as one would
  have produced a false alarm.** Packing `master` in the WSL clone gives
  `0c48aa5e…`, not the registry's `34c94881…` — gzip settings and archive mtimes
  differ between hosts. The equivalence that means something is _content_: same
  files, same hashes. That is what was measured, and it is why the two commits
  below could be compared at all.

- **the artifact-equivalence waiver for the red `windows-unit` was earned, one
  condition at a time.** `6589db36` is 4/5 green with `windows-unit` red. The
  owner's waiver required five things, all confirmed against immutable CI and a
  measurement taken now:
  1. `149f4870..6589db36` is **one file, journal markdown**, +494 lines — no
     product or packaged file;
  2. packing **both** commits back-to-back in one environment gives a
     **byte-identical tarball** (`sha=0c48aa5e…`, 245 files, 1660553 unpacked);
  3. `windows-unit` is **green** on the PR head `17e5afeb` and on the parent
     `149f4870`;
  4. the failed job's only test annotation is `codex.test.ts` ›
     "anchors a nested-cwd Windows Codex rulebook edit to the canonical
     repository root", `Test timed out in 15000ms` — **no AssertionError**;
  5. the other four checks on `6589db36` are green.

  The red check was **not re-run, not hidden, and is not being called green.**
  It is red, and the release rests on proven artifact equivalence instead. The
  waiver covers this docs-only commit and nothing else.

- **step 9 was run against the published artifact, not a git checkout.** Both
  targets from `npx create-agent-rig@0.7.0`: `aws-serverless` → `pnpm check`
  exit 0 (Next build + `cdk synth`), `node-service` → 16 files / 81 tests
  passed. And `npx create-agent-rig@0.7.0 upgrade --dry-run` inside a rig
  generated by published **0.6.2**: 23 to replace, **5 new**, 0 yours, 64 already
  current — the same five files `CHANGELOG.md` promises, and nothing written.

- **RP-67's criterion 6 is closed on evidence, three months after the other
  five.** It needed a published release, so it could not close at implementation
  time. Fresh downstream `ai-incident-commander` `main` (rig 0.5.0), fetched as a
  tarball, upgraded by the published 0.7.0 — `Wrote 44 files`, manifest
  0.5.0 → 0.7.0, nothing patched by hand. The neutrality scan that
  `generator-neutrality.test.ts` performs went from **4 offenders to 0**, and a
  rig from published 0.6.2 still scores **5** — including `AR-142`, `AR-124` and
  the `board RP` line this ticket was filed on. A green scan with a control that
  goes red is worth something; without one it is just an empty surface.

- ⚠ **`upgrade` exits 0 when it refuses to write.** Plain `npx … upgrade` in a
  non-interactive run prints "Refusing to rewrite files … Re-run with `--yes`"
  and returns **0**. The first downstream attempt read as a success and changed
  nothing; the scan afterwards still showed the baseline, which is what caught
  it. Anything scripting an upgrade must check the plan line, not the exit code.

- 🔴 **the repository was asserting something false, which is why this entry has
  a PR behind it.** `PLAN.md` still said "0.7.0 prepared, publish pending the
  owner" and "`0.6.2` … the current `latest`". And the `0.7.0` section of
  `CHANGELOG.md` predates RP-80: the release **contains** the fail-closed fix for
  a present-but-unreadable command — verified in the published tarball, which
  carries `command.kind === 'unreadable'` in `guard-bash.mjs` — while the section
  described only the surface-coverage fix that came before it. Both are corrected
  here. Nothing else about the released bytes changed; a changelog entry is not a
  re-release.

- **the ledger stays at 0.6.2, deliberately.** `templates/agent-os/…` aside,
  `templates/release-ledger.json` records a version's published SHA at the
  release _after_ it (`README.md`, and `CHANGELOG.md` step 7's
  `npm view create-agent-rig@<previous> gitHead`), because a release commit
  cannot contain its own SHA. 0.7.0's line belongs to the next release
  preparation, not to a PR filed for the sake of the line.

- **board reconciliation, all of it applied by hand and said so.** RP-73 closed
  `premise-rejected`: its worker-cap remedy is falsified by its own measurements
  (none → 24 failures, cap 4 → 13, cap 2 → **32** at twice the wall time — not
  monotonic), and the operational problem is resolved by policy instead, WSL on
  ext4 as the canonical gate surface. That is **not** a native-Windows fix and
  the ticket says so. Nine Done items were carrying a stale `ready` — RP-10,
  RP-48, RP-63, RP-64, RP-65, RP-66, RP-67, RP-70, RP-80 — all cleared; RP-80
  also lost a spent `owner-ruling`.

- **RP-80's two loose advisories are no longer loose.** The first reproduces on
  `master`: `edit-input.mjs` opens with `input?.tool_input ?? {}`, so a
  **present** but unreadable container flows through as an empty fragment and
  every EDIT surface allows what `apply_patch` refuses — measured, with both
  controls holding (readable object blocks, absent field allows). Filed as
  **RP-85** with `triage` only, no `ready`, deduped through the real
  `duplicateOf` against 50 bodies. The second — a future union member falling
  into the `kind !== 'string'` allow — is **unreachable today** (the union has
  three members and `unreadable` is intercepted above it), so it stays a
  documented advisory rather than becoming work.

- ⚠ **the queue adapter still cannot read the board**, so every selection figure
  here came through the real offline seam (`jira.mjs › listEligible({issues})` →
  `core.mjs › selectNext`) on live data pulled by MCP. Every filter above that
  seam ran; the network layer, the pagination walk and **every write path** did
  not. Each transition, label edit, link and comment was applied by hand.

- 🔴 **the next item is not the one the brief predicted.** The brief said to take
  RP-61 "if the live selector still selects it". It does not, under any spacing
  state: `null`/`normal`/`elevated-prose` → **RP-13**, `elevated` → **RP-2**.
  RP-13 outranks RP-61 because it `blocks` RP-16 and `sortCandidates` ranks
  unblockers first. The `ready`-labelled, owner-`rig`, eligible set is exactly
  the eight predicted (RP-55, 56, 60, 61, 68, 69, 72, 74) — but "carries `ready`"
  and "is what the selector returns" are different questions, and only one of
  them was asked. No item was taken.

### RP-80 shipped an armed kill switch that argv walked past; the Jira board was four items behind the code, not two; and RP-81's premise did not survive being measured

- **done — RP-80 merged as `149f4870`.** PR #168, two commits, squashed. Before
  it, with the brake armed, `{"command":["gh","pr","merge","1"]}` exited **0**
  from `guard-bash` before `brakeIsOn()` was ever consulted, on **both** shell
  surfaces; `block-no-verify` had the same hole by a worse road, because
  `String(argv)` comma-joins and its tokeniser never splits on a comma, so
  `--no-verify` went _invisible_ rather than unreadable. One shared contract in
  `hook-input.mjs` now decides absent / string / unreadable for both guards.

- 🔴 **the board was further from the code than the brief said.** The hypothesis
  named two stale items (RP-67 `In Review`, RP-70 `To Do`). Measured: **four** —
  RP-64 and RP-65 were also still `To Do` with their implementations merged and
  shipped in the 0.7.0 release commit. Every closure was re-measured against the
  code rather than taken from the PR body; RP-64's acceptance was re-run as a
  command (8/8 exit 2, structured `unverifiable`, no stack trace).

- 🔴 **RP-81's premise is false, and the reason is one line of reasoning nobody
  had done.** `invokedDirectly()` compares `real(A)` against `real(B)` where
  `real` is a pure function of a string — so a _one-sided_ canonicalisation
  failure needs the two sides to be different strings. Node hands both sides the
  **same** string in every direct-invocation spelling probed (absolute, 8.3
  alias, long name, lowercase drive, forward slashes, redundant separators,
  relative). The only spelling that differs at all is a symlink, and there both
  sides canonicalise to the same target. Real script: **40/40 exit 2** in WSL,
  8/8 on Windows, **0/40** runs with no output; through a symlink the body ran;
  imported as a module the body did not. Closed as premise-rejected with no
  product code changed. The in-vitest variance it was filed from is RP-73's
  spawn latency, not this boundary.

- ⚠ **the consequence is still real even though the trigger is not**, and that
  is written on the ticket rather than quietly dropped: the whole CLI body sits
  inside `if (invokedDirectly())` in **12** scripts, so a false negative there
  would exit 0 silently and turn a hold into a pass. No trigger was found, so
  nothing was patched — a fix with no reproduction is a guess.

- **the gates earned their cost again, and disagreed with each other once.**
  `security-scanner` SHIP; `code-reviewer` and `prose-reviewer` both HOLD, and
  they found the **same** blocker independently: the Contract header at the top
  of `guard-bash.mjs` still read "Fails open on anything it cannot parse", three
  lines above the import the change added. `prose-reviewer` added a second: the
  rewrite kept a measurement and dropped its pointer, where the block it
  replaced had carried both. Then the two reviewers **contradicted each other**
  on the remedy — `code-reviewer` asked for `kind !== 'string'` to stop a future
  member throwing outside the try; `security-scanner` observed that this exact
  form is fail-_open_ for a future member and nothing pins the line. Both are
  right; the second is recorded for the owner rather than fixed, because no such
  member exists and the head is no weaker than the one already reviewed.

- ⚠ **five Stop-gate runs failed on this host, and none of them was about the
  change.** All 126 failures across them are `Test timed out in 15000ms`, zero
  AssertionErrors, on a byte-identical tree — and the failing _set moved every
  time_, which a defect does not do. The controls: the same commits are green in
  CI including `windows-unit`, and give 2809 passed / 0 failed in the WSL clone.
  Load correlates directly: three concurrent reviewers took one run from 24
  failures / 275 s to **37 / 406 s**. Recorded on RP-73, whose bounded cap
  experiment is now exhausted and awaiting an owner ruling; the concurrency was
  my own choice and is RP-68's cost, paid knowingly to avoid an hour of
  sequencing.

- **unblocked** — nothing was waiting. The board does carry dependency links,
  so this is answer (2) and not answer (3): `Relates` is in use and RP-62/RP-73
  were linked with it during this run. But RP-80, RP-81, RP-64, RP-67 and RP-70
  all carry an empty `issuelinks`, and RP-65's two links (AR-105, RP-77) are
  `Relates` rather than blockers — both stay open and neither was released by
  closing it. So all **six** closed this run — RP-64, RP-65, RP-67, RP-70, RP-80 and
  RP-81 — freed nothing that was queued behind them: five carry no links at
  all, and RP-65's two are non-blocking and both still open.

- ⚠ **`human-review` is absent from #168, deliberately.** No human reviewed this
  head, so the label would record a review that did not happen.
  `detect-missed-gate` will report the merge, correctly.

### The release tail collapsed from five PRs to zero, and 0.7.0 is prepared — the gates found a real security defect in every functional PR, and five false claims in my own release note

- **done — five of five merged, then `254b25c8` on `master` carrying 0.7.0.**
  RP-17 `fca5b369`, RP-67 `f96adcd4`, RP-65 `3507f5d2`, RP-64 `ac294987`,
  RP-70 `49f39d9f`, release `254b25c8`. No open PRs. Full suite on the merged
  tree: 82 files, 2707 passed, 0 failed. Pack: `create-agent-rig@0.7.0`, 245
  files, 221 template entries, zero `test/` entries.

- 🔴 **every functional PR in the tail carried a real defect that reading the
  diff could not have found.** Each needed a behavioural probe:

  | PR    | what it turned out to be                                                                                                                                                                                                                   |
  | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
  | RP-65 | the PR did not fix its own bug: the matcher was widened but both hooks still opened with `tool_name !== 'Bash'` and returned allow, so force-push, `rm -rf /` and the pre-commit bypass all ran on the second surface with the brake armed |
  | RP-70 | the redaction it added leaked at its own truncation boundary — 68 of 500 swept cut positions published a password suffix                                                                                                                   |
  | RP-64 | a credential in the base URL reached stdout AND was persisted into the run journal; it took three rounds because the first two fixes closed the form just found instead of the class                                                       |

  Two of the three looked ready at the time: green CI, a sensible diff, and in
  two cases a human-review label already applied.

- 🔴 **the release note was the worst document in the change, and it was mine.**
  Six blockers across two gates: the novelty was oversized (0.6.2 already
  shipped the revalidation mechanism), the file count was wrong (five, not
  four — found independently by all three gates), three claims of coverage were
  wider than their mechanisms, and the residue figure counted one shipped
  exclusion of the two. One of them contradicted a measurement I had made and
  written into the code myself an hour earlier.

- 🔴 **one of those six came from my own bad measurement.** I unpacked the
  published 0.6.2, read "absent" for three revalidation files, and built the
  novelty framing on it. Re-extracting shows them present — the first
  extraction was incomplete. A reviewer caught the claim; only re-measuring
  caught why I made it. The lesson is not "check the tarball" but "an
  unexpected result is the one to repeat".

- **the guard that let a contradiction live a whole release, fixed at the
  guard.** `PLAN.md` §11 said 0.6.1 was `latest` while its own status line said
  0.6.2, and the test meant to catch that asserted `/0\.6\.2 prepared/` against
  text reading "`0.6.2` is prepared" — backticks and one word. The assertion now
  covers that spelling and pins both places positively; verified by mutation.

- **two measured instances of the RP-68 tree race, one nearly costly.** A
  security scanner's first sweep reported 56 leaks and matched its own
  pre-fix control exactly — a concurrent reviewer was mutating the same
  working tree. It re-pinned every measurement to `git cat-file` and re-ran.
  What saved it was a reviewer doubting its own numbers, which is not a
  control. Recorded on RP-68 with the evidence.

- **filed rather than fixed:** RP-80 (an `invariants.md`/`guard-bash.test.ts`
  collision over a present-but-unreadable `command`, which walks past an armed
  kill switch — owner-authorised, next), RP-81 (`invokedDirectly()` can skip the
  whole script body and exit 0, turning a hold into a pass), RP-82 (a citation
  count that drifts on every merge while asserting it is unchanged).

- ⚠ **the Stop gate fired thirteen times and never once about the change under
  test.** Every firing was 15-second timeouts with no assertion failure, and
  most were concurrent with reviewers running the suite in the same tree. The
  same trees passed in WSL and in CI every time. That is the real cost in
  RP-73: a gate that is always red carries no information, and the day it is
  right will read like the other thirteen.

- ⚠ **`human-review` is absent from every PR merged here, by choice.** The
  owner reviewed heads that later changed, so carrying the label forward would
  have recorded a review that did not happen. `detect-missed-gate` will report
  these merges, correctly.

- **publishing is not done and is not the agent's.** `npm whoami` reports no
  auth, and PLAN.md §11 makes the release an owner action needing 2FA. The
  registry still serves 0.6.2; step 9 — smoking the published artifact — cannot
  run until it does not.

### Gate round 2 on RP-67 passed both cold readers; the two blockers left were in my own prose, and one of my repairs to it was itself wrong

- **done — PR #164 at `4cbb0f78` carries `SHIP` from `code-reviewer` and
  `prose-reviewer`, 0 blockers each.** Round 1's verdicts answered for
  `96ffa7ef` and were void after the commit, so the whole gate was re-run.
  Routing recomputed from the committed diff: lane **model**, risk flag
  `elevated-path`; `verdict.mjs coverage 4cbb0f78` reports coverage complete.
  Exact-head CI is green on all six checks. **No new head was created**, so both
  verdicts still answer for the commit they were given.

- **the owner's blocker was closed by a measurement, not by agreement.** The
  narrowing to `BACKLOG_KEYS = ['AR', 'RP']` is only safe if it loses nothing:
  running the **old** broad `[A-Z][A-Z0-9]*-\d+` over the current surface
  returns **0 hits**. Round 1's header blocker was closed by 10 mutations that
  each turned the suite red, not by re-reading the header.

- 🔴 **`check-premises` held two blockers against this run's own prose, and it
  was right both times.** The PR body quoted `"measured twice"` as the node-ts
  rule's wording — a phrase that exists at neither `7cecf137` nor `4cbb0f78`,
  only at the intermediate head `96ffa7ef`. And it claimed the figure-removal
  rule "applies to two sentences" while four past-run figures survived the same
  diff after losing their citations.

- 🔴 **my repair of the second one was itself an invented rationale — the third
  time this failure mode has cost a round.** I wrote that three of the four
  figures keep a test pointer and the fourth does not. The fourth keeps one too
  (`loop/SKILL.md:853` → `queue.test.ts`, four names, all declared), sixteen
  lines away instead of three; and on the strict reading none of the four
  pointers pins the _figure_, only the mechanism. Wrong in both directions, so
  there is no reading on which 3-to-1 is right. I had verified the three
  positives by hand and inferred the fourth negative instead of checking it. The
  re-run of the gate on the repaired paragraph caught it; the body now states
  the measurement.

- **what I deliberately did not do.** `code-reviewer` advisory 3 is real — the
  header claims `.claude/agents/` specs are in the surface, and narrowing the
  filter's `(rules|skills|agents)` to `(rules|skills)` leaves all four tests
  green, so nothing pins it. Fixing it means a new head, which voids both SHIP
  verdicts and spends the last of three gate rounds on a finding neither cold
  reader held as blocking. Named on the PR for the owner instead. I also did not
  apply `human-review`, though the diff crosses four elevated paths and the
  sweep will report the merge without it.

- **reconciliation first, per the reset instruction.** Five open PRs, none
  superseded, all fast-forwardable. #160 (RP-17) is owner-approved and green on
  its exact head. #163 (RP-70) and #165 (RP-64) hold owner blockers still
  literally present in their heads — measured, not inferred from comment
  timestamps: `run.ts:210` still has `{ cause: error }`, and
  `revalidate-adapter.test.ts:283` still has `/^s+at /m` while lines 87 and 138
  in the same file use the correct `/^\s+at /m`. #166 (RP-65) is owner-clean but
  held on the red Windows lane.

- ⚠ **`claim-unavailable`, so no new item was taken.** `JIRA_BASE_URL` /
  `JIRA_EMAIL` / `JIRA_API_TOKEN` are unset; `queue/index.mjs next` reports
  `queue-unreadable`. RP-72/73/74 are owner-approved and ready, and all three
  stay untouched. Existing PRs were continued because their work has durable
  GitHub and Jira evidence.

- ⚠ **the Stop gate went red on the host, and it produced new evidence rather
  than a retry.** Full `pnpm test:unit`: 70 failed / 2542 passed, every failure a
  15 s timeout with no AssertionError, on a tree this session changed **zero**
  tracked files in. The discriminator: the file the gate named fails **one** test
  in isolation — a _different_ one than any of the seven it contributed to the
  full run — and needs 286 s alone for 74 tests against a 15 s per-test budget.
  So the failing member moves _and_ isolation does not clear it, which is the
  re-scoped RP-73 cause (per-spawn latency against a fixed budget) reproduced on
  a second file. Recorded on RP-73. Nothing was weakened, skipped, retried or
  bypassed, and no host configuration was changed.

### RP-67 is committed and pushed — the WSL clone existed all along under a name I never searched for, and the full suite is green there with no cap and no bypass

- **done — `4cbb0f78` on `fix/rp-67-generator-neutral-skills`, pushed; PR #164
  body corrected and the owner's blocker answered.** The full pre-commit ran in
  the WSL clone and passed outright: **79 files, 2628 passed, 3 skipped, 0
  failed**, 154.8 s, commit wall 207 s. No worker cap, no `--no-verify`, no
  timeout change, no assertion touched.

- 🔴 **the escalation before this one rested on a false statement of mine.** I
  reported "there is no WSL clone on this machine", citing
  `find /home /root -maxdepth 3 -name create-agent-rig -type d` returning
  nothing. The clone is `/home/user/car` — the directory is named `car`, and my
  search was for the repository's name. It is real ext4 (`/dev/sde`, its own
  inodes), its `origin` is this Windows checkout, and it was already on the same
  branch at the same head. A memory note recorded that path a day earlier; I
  searched instead of reading it. The whole cap investigation was avoidable.

- **what the two hosts actually cost**, which is why the clone is the answer:

  | host                 | bare `node -e '0'`         | full `pnpm test:unit`                          |
  | -------------------- | -------------------------- | ---------------------------------------------- |
  | Windows checkout     | 808, 930, 922, 577, 572 ms | 24–71 failed of 2631, four runs disagreeing    |
  | WSL `/home/user/car` | 743, 279, 268, 211, 255 ms | **79 files / 2628 passed / 0 failed**, 154.8 s |

- **transfer, by the route the memory note already recorded**: push from WSL to
  `refs/heads/wsl-incoming` in the Windows checkout, verify the incoming commit
  against the staged tree (`git diff --cached wsl-incoming` was empty, so the
  reset was safe), `git checkout -- .`, `merge --ff-only`, delete the side ref,
  then push to GitHub from Windows where the credential helper lives.

- **stopped at — checkpoint, still running.** CI and E2E are both registered and
  in progress on `4cbb0f78`.

- 🔴 **what is NOT done, and must not be assumed:** the reviewer verdicts
  (`code-reviewer`, `prose-reviewer`, `check-premises`) were all on `96ffa7ef`
  and **do not carry to this head** — the gate needs re-running on `4cbb0f78`
  before merge. `human-review` is not mine to apply. Revalidation still cannot
  run: `revalidate.mjs` exits with the raw stack trace from
  `requireCredentials (jira.mjs:259)` — the defect RP-64/#165 fixes, unmerged —
  and there is no claim record for RP-67.

- **unblocked — nothing yet.** RP-67 is not closed; it is committed and pushed.

- **cost** — reviewer subagents: 0 this stretch. CI runs consumed: 1 (CI + E2E on
  the new head). Full local `test:unit` runs: 3 on Windows (caps 4, 2, plus the
  gate's own) and 1 in WSL.

### The bounded worker cap was measured across three settings and does not work: the wall is per-spawn latency, not parallelism, and one test times out with nothing else running

- **done — nothing merged, nothing committed.** Continuation of the run below,
  under an owner allowance to apply a bounded worker cap so RP-67 could be
  committed. The allowance was exercised in full and the answer is negative.

- **the patch was restored and checked first, as instructed.** `git diff
--cached` is byte-identical to `.claude/runs/20260901-071813/rp-67-staged.patch`;
  HEAD unchanged at `96ffa7ef`; `master` and `origin/master` both still
  `7cecf137`; merge-base unchanged. Nothing had drifted, so there was nothing to
  re-apply. One repair was added on top — the non-vacuity test's name now
  describes what it checks — and the patch re-saved (245 lines).

- **how the cap was applied, and what was NOT touched.** Through
  `VITEST_MAX_WORKERS`, which vitest reads as the direct equivalent of
  `--maxWorkers` (`coverage.DM_a_rWm.js`). So the cap reached the process the
  hook spawns with **no change in the repository at all**: `.husky/pre-commit`
  and `vitest.config.ts` are untouched, which leaves the permanent version to
  RP-73. Unchanged and checkable from the branch diff: the 15 000 ms figures in
  `ci.yml` and `vitest.config.ts`, `vitest-timeouts.test.ts`, every assertion, the
  test set. No `--no-verify`, no disabled hook, no retry-to-green.

- 🔴 **measured — and the trend is not monotonic**, which is what falsifies the
  hypothesis. Full `pnpm test:unit`, idle tree, 22-CPU host:

  | cap       | result                               | wall       | kinds                                                          |
  | --------- | ------------------------------------ | ---------- | -------------------------------------------------------------- |
  | none (22) | 24 failed / 2588 passed, 10 files    | —          | 20 timeouts, 2 timing assertions                               |
  | 4         | 13 failed / 2599 passed, 4 files     | 559 s      | 12 timeouts (worst 23 806 ms), 0 EBUSY, 1 guard budget refusal |
  | 2         | **32 failed** / 2580 passed, 5 files | **1176 s** | 28 timeouts, 0 EBUSY, 1 guard budget refusal                   |

  24 → 13 → **32**. Halving the cap doubled the wall time and tripled the
  failures. A contention remedy does not behave that way.

- 🔴 **and one test fails with zero contention.**
  `test/template/gate-rounds.test.ts` run entirely alone: 1 failed of 23, wall
  97 s — › "exits 1 on its own failures, and says it is not an exhausted cap",
  `Test timed out in 15000ms`, recorded at 15 873 ms.

- **the cause, measured rather than inferred.** That test makes four sequential
  `run(...)` calls, each spawning a Node child, inside one 15 s budget. Spawn
  cost here: bare `node -e '0'` at 808, 930, 922, 577, 572 ms; the queue CLI at
  829, 839, 764 ms. **0.6–0.9 s per spawn**, against the 50–80 ms a Linux CI host
  pays. Four spawns plus their work do not fit, and no worker count can help,
  because those calls are sequential _inside_ the test. The earlier
  `--maxWorkers=2` result that motivated the attempt (2 failed of 413) was a
  ten-file subset that happened to exclude the worst case — my own reasoning
  error, generalising from a subset.

- **stopped at — the owner's stated stop condition**, verbatim: if the full
  pre-commit does not pass even under a bounded cap, stop and do not look for the
  next workaround. It does not, and I have not.

- 🔴 **a procedural error of my own, recorded because nothing else would catch
  it.** This continuation reused the run directory whose trace the previous stop
  had already closed with its run-end marker. `recordDecision` correctly refused
  every further record — "a second record after the end would make _when did this
  run stop_ have two answers" — so **none of the measurements above are in
  `decisions.jsonl`**; they are in `worker-cap-evidence.md` in the same directory
  and in the RP-67 comment. A continuation is a new run and needed its own
  directory declared before its first command.

- **escalated — RP-67 again**, with the measurements above, and **RP-73 amended**:
  the remedy that item names is falsified by them, so it needs re-scoping before
  anyone works it. I filed RP-73, and its evidence was a subset — that is worth
  stating plainly on the item rather than leaving for its take-up to discover.

- **unblocked — nothing.** RP-67 did not close.

- **cost** — reviewer subagents run: 0. CI runs consumed: 0 — nothing pushed.
  Full local `test:unit` runs: 3 (caps 22-baseline reuse, 4, 2) plus one
  single-file isolation run. Wall time spent on measurement: 559 s + 1176 s +
  97 s. Token and currency figures: not observed, not estimated.

### RP-67's gate round is done and its repairs are green, but nothing can be committed on this host: five red `test:unit` runs, no substantive failure in any of them

- **done — nothing merged, and nothing committed.** One item taken (RP-67), one
  gate round completed, every blocker repaired in the working tree, and the
  commit refused by its own pre-commit hook. The repairs are staged and saved as
  `.claude/runs/20260901-071813/rp-67-staged.patch` (239 lines).

- 🔴 **the adapter did not run, and this entry is not evidence that it did.**
  `JIRA_BASE_URL` / `JIRA_EMAIL` / `JIRA_API_TOKEN` are unset in this checkout.
  The board was read through the Atlassian Rovo MCP (`project = RP AND labels =
"repo-rig" AND statusCategory != Done`, 17 issues) and fed through the **real**
  adapter's offline seam — `jira.mjs` › `listEligible({ issues })` → `core.mjs` ›
  `selectNext` — so every selection filter ran unmodified on live data, while the
  network layer, the pagination walk and every adapter **write** path did not.
  The claim transition, the `escalated` label and the diagnosis comment are
  hand-written through Rovo, so **no `workflowClaim` exists and the revalidation
  chain has no claim record for this item**. `hygieneOf` over the same set
  returned `null`.

- **selection — and the adapter's answer was overridden by an owner ruling, on
  purpose.** `selectNext` returned RP-65 out of 6 candidates. The owner's
  correction comment on RP-62 (2026-08-31 18:52) sets the tail order as RP-67 →
  RP-64 → RP-65 with RP-62 a release blocker that does not reorder them, and a
  newer owner ruling outranks the adapter's priority/created sort. So RP-67 was
  taken. The five items carrying `agent-queue` + `ready` + `repo-rig` without
  `parked`/`operator-queue` are RP-62, RP-64, RP-65, RP-67, RP-68.

- **reviewed — one round, four sources, three distinct defects.** Routing:
  `decision-router.mjs --base master` → lane **model**, risk flag
  `elevated-path`; `security-scanner` not triggered.

  | gate                                                          | verdict    | blockers |
  | ------------------------------------------------------------- | ---------- | -------- |
  | `check-premises` (second entry point, on the run's own prose) | UNMEASURED | 5        |
  | `code-reviewer`                                               | HOLD       | 1        |
  | `prose-reviewer`                                              | HOLD       | 2        |
  | owner review on #164                                          | blocking   | 1        |

  The two reviewers and the owner converged on the same file from opposite ends.
  What each cost, and what fixed it:

  1. **The classifier owned a lexical shape it has no claim to** (owner).
     `TICKET = /\b[A-Z][A-Z0-9]*-\d+\b/` made every uppercase-key-plus-number
     token a tracker id, so a legitimate `RFC-3339` in a generator-neutral
     document would be rejected. Repaired as the failing test first —
     `expected [ 'RFC-3339' ] to be null` — then narrowed to a named set,
     `BACKLOG_KEYS = ['AR', 'RP']`.
  2. **The header promised cover the scan did not give** (both reviewers). It
     claimed "three exclusions, each on a stated ground rather than by omission"
     while the filter silently skipped `universal/CLAUDE.md`, `AGENTS.md`,
     `PLAN.md` and `journal/README.md`, and the ground stated for `init/*.md`
     was false. **Measured:** appending `AR-999` to `universal/CLAUDE.md` passed
     the guard silently — the document a rig reads first was the one file the
     scan never opened. Surface 25 → 31 files; the mutation now goes red naming
     file and line; the six additions and the four exclusions are each asserted.
  3. **Two sentences kept a measurement while losing what located it**
     (`check-premises` + `prose-reviewer`). Deleted per `invariants.md`.

  Narrowing the classifier fixed an advisory both reviewers had filed
  independently: the header's excluded-scripts figure, 71 citations of 24
  distinct ids across 22 files, is now really what "the same regex this file
  scans with" yields — the wide pattern gave 77/26/23 because it also matched
  `UTF-8` and a `Z0-9` inside a character class.

- 🔴 **escalated — RP-67, `documented-stall`. The stage is the pre-commit hook;
  the wall is that `pnpm test:unit` does not reach green on this host.** Five red
  runs, and **not one substantive failure**:

  | run                              | conditions                               | result                   |
  | -------------------------------- | ---------------------------------------- | ------------------------ |
  | pre-commit #1                    | other work in flight                     | 35 failed / 12 files     |
  | diagnostic                       | Stop gate ran its own suite concurrently | 63 failed / 16 files     |
  | Stop gate                        | concurrent with the above                | 59 failed / 16 files     |
  | pre-commit #2                    | **alone**                                | 24 failed / 10 files     |
  | those 10 files, `--maxWorkers=2` | **alone**                                | **2 failed / 413 tests** |

  The solo full run's 24 are 20 `Test timed out` plus exactly two assertions,
  both about time:

  ```
  AssertionError: deep braces as a decoy took 5931ms: expected 5931 to be less than 3000
  AssertionError: expected 2 to be +0 // Object.is equality
  ```

  Earlier runs also carried `EBUSY: resource busy or locked, rmdir
'C:\...\Temp\caf-cli-report-Vw5MsQ'` and six of the form `expected 'BLOCKED —
cannot safely inspect this …' to match /core/i` — guards giving their
  documented fail-closed refusal on hitting their own inspection budget under
  load, not finding a violation.

  Why the host and not the change, each point measured: every failing file spawns
  subprocesses; `codex.test.ts` and `guard-rulebook.test.ts`, which carried all
  six of those guard assertions, run in isolation at 130 passed / exit 0; the
  failing SET differs between runs of the same tree (12, 16, 16, 10) and a defect
  reproduces where contention does not; capping workers cuts 24 to 2; the change
  is prose in four documents plus one test and imports nothing any failing test
  touches; and CI on the previous head of this same branch was green on all six
  checks including `windows-unit`.

  The question left for the owner: **where should RP-67 be committed from?** The
  previous sessions' answer was a WSL clone, and `find /home /root -maxdepth 3
-name create-agent-rig -type d` finds none on this machine.

- **a self-inflicted cost worth recording, because it wasted two runs.** The
  Stop gate runs `pnpm test:unit` itself whenever a turn ends. While a suite was
  already running, that produced two concurrent full suites — which is why the
  diagnostic run and the Stop gate run are the two worst rows in the table above,
  and why the first `--maxWorkers=2` experiment had to be discarded and redone in
  the foreground. A run measuring this host's suite must not let its turn end
  while the suite is in flight.

- **stopped at — a systemic wall, run-scoped.** Not the two-in-a-row escalation
  stop (the count is 1). Every item in this repository ends in a commit, and the
  commit is what cannot complete, so the next item would hit the identical wall.
  `.claude/rules/autonomy.md` ("Never") rules out `--no-verify` and rules out
  weakening a test or a timeout to get green; every remaining hypothesis is a
  rulebook or Tier-2 change that belongs to the owner.

- **unblocked — nothing.** RP-67 did not close. The Jira queue does carry
  dependency links and the item has none, so nothing was waiting on it.

- **queue hygiene — `hygieneOf` reported `null` over the 17-issue `repo-rig`
  set.** Reported separately, not corrected: **four PRs are open against items
  whose Jira status was `To Do`** — #163 (RP-70), #164 (RP-67), #165 (RP-64),
  #166 (RP-65). Work was in flight while the board read as available, which is
  the state the claim step exists to prevent; the cause is the credential gap
  above, since no session in this checkout can call the adapter's `claim`.

- **proposals — three, the cap.** Bodies are `triageItemFor`'s exact output,
  each through `validateProposal` (including `checkInferenceAgainstMeasurement`)
  and the real `duplicateOf` against RP-55, RP-56, RP-59, RP-60, RP-61 — no
  match. Filed by hand through Rovo, since `proposeTriage` cannot reach the
  board:
  - **RP-72** — `codex.test.ts:1545` mkdtemps into the repository root.
  - **RP-73** — the local pre-commit `test:unit` cannot finish on this host;
    give it the worker cap RP-62 ruled for the hosted lane. This is the wall
    above, and it is what unblocks RP-67.
  - **RP-74** — decide whether the 71 tracker citations shipping in
    `.claude/scripts` and `.claude/hooks` are the same neutrality defect, since
    RP-67's acceptance 6 cannot be proven from this repository.

- **cost** — reviewer subagents run: 3 (`check-premises`, `code-reviewer`,
  `prose-reviewer`). CI runs consumed: 0 — nothing was pushed. Deploys
  triggered: 0. Full local `test:unit` runs: 5, of which 2 were self-inflicted
  concurrency. Token and currency figures: not observed by this session, and
  deliberately not estimated.
