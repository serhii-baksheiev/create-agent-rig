node_modules/
dist/
coverage/
cdk.out/
templates/
pnpm-lock.yaml
PLAN.md
# synced from templates/agent-os by scripts/sync-agent-os.mjs — formatting is
# owned by the sources (the addendum under .claude/ is hand-maintained but
# composed verbatim into CLAUDE.md, so it must not be reformatted either)
CLAUDE.md
.claude/
AGENTS.md
.agents/
.codex/
# ...and the synced set reaches outside .claude/ in two places, both composed
# from templates/agent-os/universal/ (itself ignored above): journal/README.md
# since AR-64, and docs/decisions/ since AR-63. The month files next to the
# first are this repo's own and ARE formatted; there is nothing of this repo's
# own next to the second yet, so the whole directory is the synced payload.
journal/README.md
docs/decisions/
