{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:create-agent-rig:conformance:v1:version-handshake",
  "title": "Command contract v1.0 — the version handshake (`--version --json`)",
  "description": "What every conforming bin answers before a consumer interprets anything else: its own name, its release version, and the contract version it implements. Additive keys are tolerated; the consumer compares the major of contractVersion alone.",
  "type": "object",
  "required": ["schemaVersion", "name", "version", "contractVersion"],
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "name": { "type": "string", "minLength": 1 },
    "version": { "type": "string", "minLength": 1 },
    "contractVersion": { "type": "string", "pattern": "^\\d+\\.\\d+$" }
  }
}
